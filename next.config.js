/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "uhdtv.io",
      "upload.wikimedia.org",
      "mango.blender.org",
      "download.blender.org",
    ],
  },
};

module.exports = nextConfig;
